{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing over the load-bearing numeric core. Coverage says these lines RAN; mutation says the assertions actually PIN the value. Deliberately narrow: these are the formulas where a wrong number is a silent scientific error rather than a visible crash (compensated sums, quantiles, Horn slope/aspect, cut/fill integration, CRS unit factors). Widen the mutate list as the decomposition lands; do not lower the threshold to make a run pass.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.stryker.config.ts"
  },
  "reporters": [
    "clear-text",
    "progress",
    "json"
  ],
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/process/numerics.ts",
    "src/terrain/quantile.ts",
    "src/terrain/ground/terrainDerivatives.ts",
    "src/render/measure/stockpileVolume.ts"
  ],
  "_thresholds_comment": "break 75 sits under the measured 87.23 so a real weakening fails while equivalent mutants do not hold the gate hostage. Some survivors here are provably EQUIVALENT, not gaps: quantile's early returns (n===1, lo===hi) and its p-clamp boundaries produce identical results through the general path, so no test can kill them. Judge a run by NEW survivors, not by chasing 100.",
  "thresholds": {
    "high": 90,
    "low": 80,
    "break": 75
  },
  "timeoutMS": 60000,
  "concurrency": 4,
  "tsconfigFile": "tsconfig.stryker-absent.json",
  "ignorePatterns": [
    ".venv",
    "node_modules",
    "dist",
    "release",
    ".stryker-tmp",
    "benchmark-results",
    "docs-site/.vitepress/dist",
    "playwright-report",
    "test-results",
    ".claude"
  ],
  "dryRunTimeoutMinutes": 30,
  "_ts7_comment": "tsconfigFile names a path the project does not carry, so Stryker skips its tsconfig rewrite. That rewrite calls ts.parseConfigFileTextToJson, which TypeScript 7 removed. Nothing here needs the rewrite: the sandbox resolves through the copied tree. Remove this once Stryker supports TypeScript 7.",
  "_scope_comment": "Four files, measured at 434 mutants. The ceiling is arithmetic, not preference: 188 mutants took 2 h 15 m in CI, so a hosted job's six-hour limit fits roughly 487. Measured alternatives were base+groundFilter at 599 and base+cellConfidence at 605, each about 7.2 h, so neither can finish. An eight-file attempt instrumented 2,482 mutants, near thirty hours, and died on the dry run before mutating anything. Size a widening by instrumenting it (stryker run --dryRunOnly reports the count in about a minute) rather than by counting lines: three times the source produced thirteen times the mutants. A score over four files is not comparable with one over three."
}
