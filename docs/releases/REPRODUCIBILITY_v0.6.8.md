# Reproducibility: OpenLiDARViewer v0.6.8

The version-agnostic toolchain, pinning, and method are in [REPRODUCIBILITY.md](../../REPRODUCIBILITY.md). This note fixes the figures reported for this release to the commands that produce them, so a reviewer can regenerate each rather than take it on trust.

## Validation baseline

The frozen validation snapshot under `validation/snapshot/` is the 0.6.2 validation baseline, carried forward. 0.6.3 through 0.6.8 added features and evidence but did not re-run the cross-platform portability workflow or a defect audit, so the snapshot's defect registry, portability record (`docs/validation/evidence/portability-v0.6.2/`), and build-identity are the 0.6.2 ones, unchanged because the runs that would refresh them were not repeated. Its checks still hold on its own bytes; it is a baseline this release inherits, not a fresh 0.6.8 run. v0.6.8 promotes no claim, so no new study feeds this inherited baseline either. The register stands at 17 products at E4 and none at E5, unchanged (see `VALIDATION_REPORT_v0.6.8.md`).

## Toolchain

- Node.js 22.18.0 (the pin in `.nvmrc`); dependencies pinned by `package-lock.json`.
- Install from a clean clone with `npm ci` (not `npm install`).

## Running the gate

```
npm run gate          # the whole battery, then a literal "GATE EXIT: <code>"
npm run test:release:tag   # the gate plus the full Playwright suite; run before tagging
```

`npm run gate` is the only command whose result should be quoted. It writes `release/gate.log`, prints its own exit status, and regenerates `docs/validation/test-evidence.json` from that log, but only when the run passed, so a failing gate cannot refresh the figures it failed to earn.

Every test count in these documents is read out of that evidence file and checked by `npm run lint:evidence`. Do not type the numbers in.

Take the exit status from the `GATE EXIT:` line, not from the last lines of output. Piping the gate into `tee` or `tail` reports the exit code of `tee`, which is essentially always 0, and the summary above it can read entirely green while the run failed. The line is written once, at the end, after every stage has reported. Read it, and nothing else, for the pass or fail verdict.

## Canonical toolchain

Every published figure is produced on one pinned toolchain. Reproducing on a different runtime is fine and useful, but it is not the canonical record.

```
Node  22.18.0   (the 22.x build that ships the pinned npm)
npm   10.9.3    (package.json `packageManager`)
```

```bash
git checkout v0.6.8
nvm use                 # reads .nvmrc
npm ci
git diff --exit-code -- package-lock.json   # a clean install must not rewrite it
npm run gate                                 # require the literal GATE EXIT: 0
```

The runtime dependency set is unchanged from v0.6.6, the SBOM carries over, and `docs/project/DEPENDENCIES.md` records the re-verification for this release.

## Two kinds of evidence, and which one to cite

| Record | Where | Authoritative? |
|---|---|---|
| `docs/validation/test-evidence.json` | committed in the repo | **No**. `releaseAuthoritative: false`, a development run |
| `test-evidence-v0.6.8.json` | attached to the release | **Yes**. Generated by the exact-tag run |

Evidence is collected FROM a gate run, so committing it creates a new commit and the committed record always describes the commit before the one it ships in. The release workflow avoids that entirely by generating the record as an artifact of the tagged checkout rather than committing it.

Cite the attached file.

## Verify a downloaded release without rebuilding it

```bash
shasum -a 256 -c SHA256SUMS
npm run release:verify -- --dir <downloaded-assets>
```

See [docs/release/RELEASE_ASSETS.md](../release/RELEASE_ASSETS.md) for the full asset set and how the hash chain closes.

## Regenerate each reported figure

Test counts and the bundle size are not reproduced in this table. They live in `docs/validation/test-evidence.json` for a development run and in the attached `test-evidence-v0.6.8.json` for the release, and `npm run lint:evidence` holds the documents to them.

| Reported figure | Command |
|---|---|
| Per-bucket and total test counts | `npm run test:unit` / `test:export` / `test:terrain` / `test:ui` / `test:slow` (the large buckets run sub-sharded, so a run prints per-shard totals that sum to the recorded figures) |
| Full gate `GATE EXIT: 0` | `npm run test:release` |
| Deterministic e2e counts (blocking project) | `npm run test:e2e` |
| GPU e2e (advisory project) | `npm run test:e2e:gpu` |
| Live entry size against its ceiling | `npm run build:live && npm run check:bundle` |
| Coverage | `npm run coverage` (also a blocking stage of the release-mode gate; read the current figures from the coverage stage of `release/gate.log`) |
| Mutation score over the numeric core | `npm run mutation` |
| Building ground-support gate evaluation | `npx tsx scripts/eval-building-gate.ts` (see `docs/validation/building-support-gate-eval.md`) |
| Documentation build | `npm run docs:build` |
| SBOM (CycloneDX, root `0.6.8`, production scope) | `npx @cyclonedx/cyclonedx-npm --omit dev --output-file sbom.json` |
| Production dependency audit | `npm audit --omit dev` |

## Regenerate the validation-suite evidence

Each suite runs on its own and states what it does not cover.

| Suite | Command |
|---|---|
| Reproducibility, one seed over ten runs at zero tolerance | `npm run benchmark:repro` |
| Scaling, 50k through 1M | `npm run benchmark:scaling` |
| Cross-platform portability, one leg | `npm run benchmark:repro:portable` |
| Cross-platform portability, comparator over two legs | `npm run benchmark:compare-platforms` |
| Unit integrity | `npm run benchmark:units` |
| GPU-versus-CPU backend equivalence | `npm run benchmark:backends` (and `benchmark:backends:gpu` for the browser leg) |
| Failure recovery | `npm run benchmark:failures` |
| Provenance integrity | `npm run benchmark:provenance` |
| Contour correctness | `npm run benchmark:contours` |
| LAS round-trip fidelity | `npm run benchmark:roundtrip` |
| Archive portability | `npm run benchmark:archive-portability` |
| Seed sensitivity | `npm run benchmark:seeds` |
| Clean clone | `npm run benchmark:clean-clone` |

## The two-platform reproducibility result

The tracked result is at `docs/validation/evidence/portability-v0.6.2/`: `status: reproduced`, platforms darwin-arm64 and linux-x64, from one workflow run at one commit. The seeded source cloud hashes identically on both, then 15 artifact hashes and 18 scalars are compared at a tolerance of exactly zero, and none differ. Host, timing and build-identity fields differ and are published per platform rather than pooled.

Scope: two little-endian platforms, one commit, one synthetic seeded fixture. Windows is untested, no big-endian host has run a leg, and no real scan data is in the comparison.

## Environment notes

- The `slow` bucket spins up the WASM LAZ decoder; the runner caps workers at 2 and raises the per-test timeout for it.
- Four e2e specs skip when the optional autzen COPC fixture is not on disk. That is a conditional skip, not a failure.
- The e2e suite is split into two Playwright projects. `deterministic` blocks; `gpu` (specs tagged `@gpu`) is advisory because its result depends on the WebGPU adapter a runner exposes. An untagged spec lands in the blocking project.
- `npm run coverage` excludes `tests/terrainRunnerDensityWiring.test.ts`. Under v8 instrumentation each of its cases takes about 75 s. It still runs in the release buckets, so nothing goes unexercised; only the coverage measurement omits it.
- Coverage is a blocking stage of the release-mode gate and its output ships inside `gate.log`. Mutation is deferred to a scheduled workflow, and the record cites that run rather than executing it at tag time.
- Browser-observable behaviour (streaming visuals, GPU paths, the WebGL 2 fallback) is exercised by the e2e suite; a green GitHub Actions run on the tagged commit is the gating browser evidence for publication.
