{
  "schemaVersion": 1,
  "quantity": "terrain evidence GeoTIFF (support_count, interpolation_distance, cell_state, nearest_support_distance, vertical_dispersion, edge_distance)",
  "candidate": "src/terrain/export/demEvidence.ts, src/terrain/export/demGeoTiff.ts and the per-cell dispersion in src/terrain/ground/rasterizeDtm.ts",
  "fixture": "validation/terrain-evidence/fixture/terrain_evidence.tif with validation/terrain-evidence/fixture/expected.json",
  "fixtureCheck": "tests/demEvidence.test.ts fails when the committed fixture differs from what the writer produces",
  "oracles": [
    {
      "oracleId": "olv-terrain-evidence-geotiff-py",
      "role": "standard-conformance-validator",
      "invocation": "python3 validation/terrain-evidence/oracle/evidence_geotiff.py --check",
      "parameterEquivalence": "Reads the committed file bytes; compares band count, sample types, tags, georeferencing and every cell against expected.json."
    },
    {
      "oracleId": "olv-terrain-evidence-geotiff-py",
      "role": "analytic-truth",
      "invocation": "python3 validation/terrain-evidence/oracle/evidence_geotiff.py --check",
      "parameterEquivalence": "Recomputes interpolation distance (Chebyshev) and nearest-support distance (Euclidean, brute force) to the nearest cell with support_count > 0, survey-edge distance (4-connected, NoData cells and measured grid-edge cells as the boundary), vertical dispersion (median absolute deviation of the listed returns) and the measured, edge-risk and edge-affected cell_state rules; --check confirms a perturbed value in bands 2 to 6 fails."
    }
  ]
}
