# Defect chronology

Generated by `scripts/build-defect-chronology.mjs`. Do not edit by hand. This file,
`chronology.json` and `chronology.csv` are rendered from one model in one pass, so they
cannot disagree with each other.

Registry version 0.6.2; 18 defects.

A field is "unknown" when neither the registry nor git establishes it. No value here is estimated, and datePrecision is never "exact" because no dated discovery record exists.

## Per-defect chronology

| defect | discovery source | discovery commit | date precision | first failing validation | basis | fix | regression test | replay | mutation | released impact | confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OLV-DEF-001 | discovery-by-review | f27163e | commit-order-only | f27163e | retrospective-replay | 9ecdae8 | 76dd71b | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |
| OLV-DEF-002 | prospective-exposure | 76dd71b | commit-order-only | f27163e | retrospective-replay | 76dd71b | 76dd71b | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |
| OLV-DEF-003 | prospective-exposure | 76dd71b | commit-order-only | f27163e | retrospective-replay | 76dd71b | 76dd71b | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |
| OLV-DEF-004 | discovery-by-review | f27163e | commit-order-only | f27163e | retrospective-replay | ee0526b | 488be9e | 722b7b5 | unknown | released-through-v0.6.1 | high |
| OLV-DEF-005 | discovery-by-review | f27163e | commit-order-only | f27163e | retrospective-replay | ee0526b | 40fb9e3 | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |
| OLV-DEF-006 | discovery-by-review | f27163e | commit-order-only | f27163e | retrospective-replay | ee0526b | 488be9e | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |
| OLV-DEF-007 | discovery-by-review | f27163e | commit-order-only | unknown | unknown | ee0526b | 4b7d188 | 722b7b5 | unknown | released-through-v0.6.1 | medium |
| OLV-DEF-008 | discovery-by-review | f27163e | commit-order-only | f27163e | retrospective-replay | ee0526b | 488be9e | 722b7b5 | unknown | released-through-v0.6.1 | high |
| OLV-DEF-009 | prospective-exposure | 1003377 | commit-order-only | f27163e | retrospective-replay | 1003377 | 1003377 | 722b7b5 | unknown | released-through-v0.6.1 | high |
| OLV-DEF-010 | prospective-exposure | 1003377 | commit-order-only | f27163e | retrospective-replay | 1003377 | 1003377 | 722b7b5 | unknown | released-through-v0.6.1 | high |
| OLV-DEF-011 | prospective-exposure | 98c1e9b | commit-order-only | f27163e | retrospective-replay | 98c1e9b | 98c1e9b | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |
| OLV-DEF-012 | prospective-exposure | 98c456e | commit-order-only | f27163e | retrospective-replay | 98c456e | 98c456e | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |
| OLV-DEF-013 | prospective-exposure | 98c456e | commit-order-only | f27163e | retrospective-replay | 98c456e | 98c456e | 722b7b5 | unknown | released-through-v0.6.1 | high |
| OLV-DEF-014 | prospective-exposure | cc5b561 | commit-order-only | f27163e | retrospective-replay | cc5b561 | cc5b561 | 722b7b5 | unknown | released-through-v0.6.1 | high |
| OLV-DEF-015 | prospective-exposure | cc5b561 | commit-order-only | f27163e | retrospective-replay | cc5b561 | cc5b561 | 722b7b5 | 05ad793 | partially-released | high |
| OLV-DEF-016 | prospective-exposure | cc5b561 | commit-order-only | f27163e | retrospective-replay | cc5b561 | cc5b561 | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |
| OLV-DEF-017 | prospective-exposure | cc5b561 | commit-order-only | f27163e | retrospective-replay | cc5b561 | cc5b561 | 722b7b5 | unknown | released-through-v0.6.1 | high |
| OLV-DEF-018 | prospective-exposure | 214d23b | commit-order-only | f27163e | retrospective-replay | 214d23b | 214d23b | 722b7b5 | 05ad793 | released-through-v0.6.1 | high |

## Notes

### OLV-DEF-001 — RFC 7946 third ordinate written in source units

Discovery is bounded by the commit that added KNOWN_LIMITATIONS_v0.6.1.md, which records the gap. The review itself is undated in git. Mutation M03 targets a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "EPSG:4979 with a foot vertical factor writes the METRE equivalent into the ordinate" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-002 — Contour set reported the requested interval, not the emitted one

Discovery is bounded by the commit that first added tests/benchmark/contourCorrectness.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding. Mutations M03, M04, M05 target a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "an over-fine interval thins the levels and the declared interval follows" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-003 — A flat surface produced an unexplained empty contour set

Discovery is bounded by the commit that first added tests/benchmark/contourCorrectness.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding. Mutations M04, M05 target a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "a flat surface emits no contour geometry" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-004 — Contour deliverable GeoTIFF omitted the vertical-unit key

Discovery is bounded by the commit that added KNOWN_LIMITATIONS_v0.6.1.md, which records the gap. The review itself is undated in git.

First failing validation: Probe "maps a metres-per-unit factor to its GeoTIFF unit code, or to none" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-005 — Async and GPU derivative path took no vertical-unit factor

Discovery is bounded by the commit that added KNOWN_LIMITATIONS_v0.6.1.md, which records the gap. The review itself is undated in git. Mutation M06 targets a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "async derivatives convert the rise with the vertical unit factor, like derivativesSync" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-006 — geodesicFill mixed horizontal and vertical units in its step cost

Discovery is bounded by the commit that added KNOWN_LIMITATIONS_v0.6.1.md, which records the gap. The review itself is undated in git. Mutation M01 targets a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "takes the horizontal step in METRES, per axis, and the rise in metres too" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-007 — Hillshade convenience wrappers hard-coded square cells and no vertical scale

Discovery is bounded by the commit that added KNOWN_LIMITATIONS_v0.6.1.md, which records the gap. The review itself is undated in git.

First failing validation: No replay baseline for this defect exits non-zero, so no commit in this repository is established as failing validation.

### OLV-DEF-008 — Despike blunder floor compared a metres constant against native-unit heights

Discovery is bounded by the commit that added KNOWN_LIMITATIONS_v0.6.1.md, which records the gap. The review itself is undated in git.

First failing validation: Probe "measures the floor in METRES against native-unit heights" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-009 — PDF report footprint read a Y-up scan's extents as Z-up

Discovery is bounded by the commit that first added tests/benchmark/unitIntegrity.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding.

First failing validation: Probe "report footprint is the same for a Y-up and a Z-up cloud of the same building" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-010 — Streaming density tier skipped the CRS unit conversion the static path applied

Discovery is bounded by the commit that first added tests/benchmark/unitIntegrity.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding.

First failing validation: Probe "the static and streaming refreshers agree on the density tier for one foot-CRS box" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-011 — LAS 1.4 vertical CRS discarded when a WKT record was present

Discovery is bounded by the commit that first added tests/benchmark/roundtripFidelity.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding. Mutation M12 targets a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "LAS 1.4 WKT survives, coexisting with the vertical GeoKeys" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-012 — A declared linear unit that could not be resolved was answered as metre

Discovery is bounded by the commit that first added tests/benchmark/failureRecovery.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding. Mutation M12 targets a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "a projected CRS declaring a unit code we cannot resolve is tagged unknown, not metres" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-013 — Three quantities were reported as measured when nothing was measured

Discovery is bounded by the commit that first added tests/benchmark/failureRecovery.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding.

First failing validation: Probe "a zero-extent cloud is given no floor area, and no density derived from one" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-014 — A prototype key resolved as a method id

Discovery is bounded by the commit that first added tests/benchmark/provenanceIntegrity.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding.

First failing validation: Probe "an unregistered method id is refused, including a prototype key" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-015 — The benchmark result verifier was blind to the files it publishes

Discovery is bounded by the commit that first added tests/benchmark/provenanceIntegrity.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding. Mutations M07, M08 target a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect. Registry affectedVersion: 0.6.1 for the artifact hashing; the verifier itself was added after v0.6.1

First failing validation: Probe "a benchmark artifact keyed `__proto__` keeps a distinct hash" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-016 — The contour GeoJSON dropped run warnings and worded the standing limitation its own way

Discovery is bounded by the commit that first added tests/benchmark/provenanceIntegrity.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding. Mutation M03 targets a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "the GeoJSON carries every warning the run produced, model and provenance alike" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-017 — A LAS file could not name the build that produced it

Discovery is bounded by the commit that first added tests/benchmark/provenanceIntegrity.test.ts, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding.

First failing validation: Probe "a LAS/LAZ export names the build that produced it, in 32 bytes" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.

### OLV-DEF-018 — The source archive shipped release pages whose include target was excluded

Discovery is bounded by the commit that first added scripts/verify-archive-portability.mjs, the suite the registry credits with the finding. Git establishes the ordering, not the day of the finding. Mutation M09 targets a file in this defect's code path. The link is file-level; the campaign does not claim to reproduce this defect.

First failing validation: Probe "unknown" exits non-zero against v0.6.1 with the regression case injected. The run post-dates the fix; it establishes that the defect was reachable at that commit, not when it was found.
